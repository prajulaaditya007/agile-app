import { Priority } from "@/state/api";

type Props = {
  priority: Priority;
};
const index = ({priority}: Props) => {
    // 6:56
    return (
        <div>index</div>
    );
};
export default index;
